# Rename this file to .env and fill in the correct Google OAuth credentials.
# DO NOT commit the .env file with real secrets to public version control!

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback
PORT=3000

# chore(env): add default environment variable templates for local deployment
